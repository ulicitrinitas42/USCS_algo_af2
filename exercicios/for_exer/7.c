#include <stdio.h>

int main()
{

    int num = 0;
    int soma = 0;

    printf("A soma da sequencia: ");

    for(int i = 0; i <= num; i++)
    {
        printf("%d, ", i);

        soma += num;
    }

    printf("eh %d\n", soma);

    return 0;
}
